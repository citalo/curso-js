let caixa=500;
console.log(caixa);

caixa= caixa + 100; //forma extensa não usual
console.log(caixa);

caixa+=200;//forma usual de adição a uma variável
console.log(caixa);

caixa-=200; //forma usual de subtração a uma variável
console.log(caixa);

console.log(`O valor do caixa no final do dia é de ${caixa}`);